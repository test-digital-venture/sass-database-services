# PostgreSQL configuration example
listen_addresses = '*'    # Allow connections from any IP address
max_connections = 200     # Increase max connections
shared_buffers = 256MB    # Increase memory usage
work_mem = 64MB           # Memory to use for query operations
