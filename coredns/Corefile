.:53 {
    # Resolve *.myapp.local to 127.0.0.1
    template IN A myapp.local {
        answer "{{ .Name }} 60 IN A 127.0.0.1"
        fallthrough
    }

    # Forward everything else to upstream DNS
    forward . 8.8.8.8 8.8.4.4
    log
    errors
    cache 30
}
